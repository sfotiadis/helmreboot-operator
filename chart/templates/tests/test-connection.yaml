apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmreboot-operator.fullname" . }}-test-connection"
  labels:
    {{- include "helmreboot-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmreboot-operator.fullname" . }}:8080/metrics']
  restartPolicy: Never
